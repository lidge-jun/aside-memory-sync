# 에이전트용: 메모리 병합 충돌 해결 지침

`./sync.sh` 가 충돌을 남기고 멈췄을 때, 아래 내용을 에이전트에게 그대로 넘기면 된다.

---

## 프롬프트

너는 Aside 메모리 저장소의 병합 충돌을 해결한다.

배경: 이 저장소는 여러 기기의 에이전트 기억을 담은 마크다운 모음이다. 자동 병합 드라이버가 이미
append-only 성격의 내용(일별 로그, `## History`)은 전부 합쳐놓았다. 지금 남은 충돌은
**서로 다른 기기가 같은 주제에 대해 다른 결론을 적어서**, 기계가 판단할 수 없던 것들이다.

작업:

1. `git diff --diff-filter=U --name-only` 로 충돌 파일 목록을 확인한다.
2. 각 파일에서 `<<<<<<< ours (this machine)` / `=======` / `>>>>>>> theirs (other machine)` 구간을 찾는다.
3. 아래 원칙으로 해결한다.

### 해결 원칙

**정보를 버리지 마라.** 기본은 통합이지 선택이 아니다.

- **양쪽이 서로 다른 사실을 말할 때**: 둘 다 참일 수 있는지 먼저 본다. 대개 같은 대상의
  다른 측면이라 한 문장으로 합칠 수 있다.
  - 예: "리눅스 전용 빌드다" + "업스트림이 중단됐다"
    → "리눅스 전용 빌드이며, 업스트림은 중단된 상태다"

- **양쪽이 정면으로 모순될 때**: 시간 정보를 근거로 최신을 택하되, 낡은 쪽은 버리지 말고
  같은 파일의 `## History` 에 날짜와 함께 남긴다.
  - 예: "팔로워 307명" vs "팔로워 512명" → Current 는 최신값, History 에 이전 관측을 기록

- **한쪽이 더 구체적일 때**: 구체적인 쪽을 택한다. 다만 다른 쪽에만 있는 조건이나 단서는 흡수한다.

- **판단 근거가 부족할 때**: 임의로 고르지 마라. 양쪽을 병기하고 `<!-- 확인 필요: ... -->`
  주석을 달아 사람이 나중에 볼 수 있게 한다.

### 파일별 주의

- `MEMORY.md`, `USER.md`: 에이전트의 기본 동작과 사용자 프로필. 항목은 되도록 모두 보존한다.
  같은 뜻의 중복 항목만 하나로 줄인다.
- `projects/`, `sites/`, `people/`, `companies/`: `## Current` 는 "지금의 안정적인 이해",
  `## History` 는 시간순 기록이다. Current 를 정리했다면 그 근거가 History 에 남아 있는지 확인한다.
- `episodic/*.md`: 여기서 충돌이 났다면 드문 경우다. 일별 로그는 append-only 이므로
  **양쪽 항목을 모두 유지**하고 시간순으로 정렬한다.

### 마무리

- 충돌 마커(`<<<<<<<`, `=======`, `>>>>>>>`)가 하나도 남지 않았는지 확인한다.
- 마크다운 구조(제목 계층, frontmatter)가 깨지지 않았는지 확인한다.
- `git add <해결한 파일>` 후 `git commit` 한다.
- 커밋 메시지에는 무엇을 어떻게 판단했는지 한 줄로 남긴다.
- 마지막으로 `./sync.sh` 를 다시 실행한다.

---

## 참고: 충돌 파일 한번에 보기

```bash
git diff --diff-filter=U --name-only | while read -r f; do
  echo "=============== $f ==============="
  cat "$f"
done
```
